[pytest]
testpaths = tests
python_files = test_*.py *_test.py security_*.py
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    security: security smoke tests
    integration: requires DB/network
